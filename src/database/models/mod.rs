// 暂时注释掉未使用的导入，保留结构以便未来扩展
// pub use common::{CommonRequest, CommonResponse, BatchRequest, BatchResponse, Condition, SoftDeleteConfig, EncryptionConfig, HealthResponse, ErrorResponse};

pub mod common;
